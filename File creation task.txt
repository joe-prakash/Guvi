Task 1:

mkdir my_folder
cd my_folder
echo "Hello from my_file.txt file" > my_file.txt
echo "Hello from another_file.txt file" > another_file.txt
cat another_file.txt >> my_file.txt
cat my_file.txt
ls -l


Task 2:

touch myfile{1..20}.txt
for i in {1..5}; do  mv myfile${i}.txt myfile${i}.yml done
ls -lt | head -n 5
